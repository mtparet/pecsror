<%= form_for @image, :html => {:multipart => true} do |f| %>
  <p>
    <label>image n°1, nom : </label>
    <%= f.text_field :name %>
    <%= image_tag(@image.image_file.thumb.url) if @image.image_file? %>
    <%= f.file_field :image_file %>
    <%= f.hidden_field :image_file_cache %>
    <%= f.submit %>
  </p>
<% end %>
